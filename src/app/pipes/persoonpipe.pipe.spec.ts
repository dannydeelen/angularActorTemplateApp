import { PersoonpipePipe } from './persoonpipe.pipe';

describe('PersoonpipePipe', () => {
  it('create an instance', () => {
    const pipe = new PersoonpipePipe();
    expect(pipe).toBeTruthy();
  });
});
